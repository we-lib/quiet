{
  "name": "quiet",
  "version": "0.0.2",
  "description": "Loads and saves json data from/to fs",
  "repository": "git@github.com:fritx/quiet.git",
  "scripts": {
    "test": "mocha"
  },
  "devDependencies": {
    "mkdirp": "^0.5.0",
    "underscore": "^1.7.0"
  }
}
